name: my_heb_clone
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.3
  provider: ^6.0.0
  dots_indicator: ^2.0.0 # TODO replace with TabPageSelector from flutter https://medium.com/flutter-community/how-to-code-an-introduction-view-in-flutter-6a6e8e510d5e
  sizer: ^2.0.15
  google_maps_flutter: ^2.0.11
  intl: ^0.17.0
  scrollable_positioned_list: ^0.2.2
  enum_to_string: ^2.0.1
  flutter_secure_storage: ^4.2.1
  modal_bottom_sheet: ^2.0.0

dev_dependencies:
  build_runner: ^2.1.2
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/grocery_images/
